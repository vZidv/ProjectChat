<Window x:Class="ChatClient.View.CreateRoomView"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
        xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
        xmlns:ui="http://schemas.modernwpf.com/2019"
        xmlns:local="clr-namespace:ChatClient.View"
        xmlns:vm="clr-namespace:ChatClient.ViewModels"
        mc:Ignorable="d"
        ResizeMode="NoResize"
        WindowStartupLocation="CenterScreen"
        Title="Создать комнату"         
        Height="250" 
        Width="400">
    
    <Window.DataContext>
        <vm:CreateRoomViewModel/>
    </Window.DataContext>
    
    <Grid Background="{StaticResource AcrylicBackgroundFillColorBaseBrush}">
        <Grid.RowDefinitions>
            <RowDefinition />
            <RowDefinition Height="0.5*"/>
        </Grid.RowDefinitions>
        
        <Grid>
            <TextBox
            Text="{Binding RoomName, Mode=TwoWay, UpdateSourceTrigger=PropertyChanged}"
            Grid.Row="0"
            ui:ControlHelper.PlaceholderText="Название комнаты"
            Style="{StaticResource DefaultTextBoxStyle}"
            Margin="20"/>
        </Grid>
        <Button
            Command="{Binding CreateRoomCommand}"
            Content="Создать комнату"
            Grid.Row="1"
            HorizontalAlignment="Center"
            VerticalAlignment="Center"
            Style="{StaticResource AccentButtonStyle}"/>
    </Grid>
</Window>
